# final-lms-spi

need for create complete new project 
- npm init -y
- npm install express mongoose cors dotenv nodemon
- npm install -D typescript ts-node-dev @types/node @types/express @types/cors
- npx tsc --init
- npm install -g typescript  (installing typescript for globaliy)
- Change type on json file to "type": "module",

and only need for node modules for from githubs 


